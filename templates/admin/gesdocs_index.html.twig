{% extends 'base.html.twig' %}

{% block title %}GESTION DOCUMENTATIONS{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>GESTION DES DOCUMENTATIONS</h1>
    <br>
    </br>
    {% for doc in docs %}
    <div>
        <h2><a href="{{ path('app_admin_gesdocs_detail', {id: doc.id}) }}">{{ doc.doctit }} </a></h2>
        <p>créé le {{ doc.createdAt|date }} </p>
    </div>
    {% endfor %}
    <div>
        <h2><a href="{{ path('app_admin_gesdocs_ajout') }}"> Ajout de Documentation</a></h2>
        <p>  --------------        </p>
        <h2><a href="{{ path('app_admin')}}">retour espace administrateur</a></h2>
    </div>

{% endblock %}
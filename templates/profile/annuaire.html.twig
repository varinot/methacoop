{% extends 'base.html.twig' %}

{% block title %}ANNUAIRE MEMBRES{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>ANNUAIRE DES MEMBRES </h1>
    <br>
    </br>
    {% for user in users %}
    <div>
    
    {#<h2><a href="{{ path('app_profile_depots_detail', {id: depot.id}) }}">{{ depot.depotit }} </a></h2>#}
        
        <p> nom {{ depot.user.nom }} </p>
    </div>
    {% endfor %}
    <div>
        
        <p>  --------------        </p>
        <h2><a href="{{ path('profile')}}">retour espace membre</a></h2>
    </div>  
</div>
{% endblock %}
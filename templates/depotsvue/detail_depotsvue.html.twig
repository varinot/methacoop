{% extends 'base.html.twig' %}

{% block title %}Détail Dépôt Vue {% endblock %}

{% block body %}
    <style>
    </style>
<div class="row">
    <div class="col-md-10 mx-auto">
        <br>
        <h1>{{ depot.depotit }}</h1>
        <br>
       <p>{{ depot.depoavis }}</p>
       <p>{{ depot.deporef }} . ->   Mail de correspondance :   {{ depot.depocorres }} </p>
       <p>Créé le {{ depot.createdAt|date('d-m-Y')}} </p> 
        <p>Mis à jour le {{ depot.updatedAt|date('d-m-Y')}} </p> 
        <br>
        <img src="{{ (depot.imageName ? vich_uploader_asset(depot) : asset('build/
        images/placeholder.jpg')) }} ">
        <br></br>
      
        <br>
        <a href="{{ path('app_depotsvue_index')}}">retour liste des documentations déposées par les membres</a><br>
        <br>
        <br></br>
        
    </div>
</div>    
    {% endblock %}